 To Set-up

 install ruby through your package manager:
 
 `apt-get install ruby`
 
 `cinst ruby`
 
`bundle install`

 To test locally

 `bundle exec jekyll serve --watch`

 then browse on

 `localhost:4000`
